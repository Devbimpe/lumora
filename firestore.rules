rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection - allow access to own data, everything else must go through API routes
    match /users/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
    }
  }
}
